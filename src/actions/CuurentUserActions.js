export const CurrentUserActions = (user) => ({type: "SET_CURRENT_USER", payload: user})

export const userLogin = (userInput) => {
    return {
        // fetch to backend to find or create by user
    }
}